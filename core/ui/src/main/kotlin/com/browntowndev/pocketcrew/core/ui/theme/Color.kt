package com.browntowndev.pocketcrew.core.ui.theme

import androidx.compose.ui.graphics.Color

// --- ACCENTS (Dark Theme) ---
val PeachAccent = Color(0xFFF4A361)
val PeachVariant = Color(0xFFB37340)

// --- ACCENTS (Original Light Theme) ---
val PurpleLightPrimary = Color(0xFF7E22CE)
val GoldVariant = Color(0xFFB45309)

// --- DARK PALETTE (Maximum Readability / Astigmatism Optimized) ---
val DarkBackground = Color(0xFF18181B)       // Lifted to Charcoal (Zinc 900) to minimize pupil dilation
val DarkSurface = Color(0xFF27272A)          // Elevated surfaces / Assistant bubbles (Zinc 800)
val DarkSurfaceVariant = Color(0xFF3F3F46)   // Input fields / User bubbles (Zinc 700)
val DarkOnBackground = Color(0xFFE4E4E7)     // Muted off-white (Zinc 200) to eliminate text halation
val DarkOnSurfaceVariant = Color(0xFFA1A1AA) // Secondary text / Metadata (Zinc 400)
val DarkOutline = Color(0xFF52525B)          // Borders / Dividers (Zinc 600)

// --- LIGHT PALETTE (Original) ---
val LightBackground = Color(0xFFFFFFFF)
val LightSurface = Color(0xFFF3F4F6)
val LightOnBackground = Color(0xFF111827)
