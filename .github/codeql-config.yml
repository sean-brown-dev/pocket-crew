name: CodeQL Configuration

# Attempting to use 'tools: linked' in workflow to support Kotlin 2.3.20
# If this fails, consider downgrading Kotlin to 2.3.10 or using java-only analysis.

disable-default-queries: false

paths-ignore:
  - third_party
  - llama-android
  - edge-gallery

kotlin:
  enabled: true
