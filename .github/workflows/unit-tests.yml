name: Unit Tests

on:
  pull_request:
    branches:
      - main
      - develop

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: wrapper
          cache-read-only: ${{ github.ref != 'refs/heads/main' && github.ref != 'refs/heads/develop' }}

      - name: Grant Execute Permission to Gradlew
        run: chmod +x gradlew

      - name: Run C++ TokenProcessor Tests
        run: |
          g++ -I llama-android/src/main/cpp llama-android/src/main/cpp/test_token_processor.cpp -o test_token_processor
          ./test_token_processor
          rm test_token_processor

      - name: Run Unit Tests
        run: ./gradlew testDebugUnitTest --parallel --build-cache -Pandroid.builder.sdk.download=true

      - name: Upload Test Reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-reports
          path: "**/build/reports/tests/testDebugUnitTest/"
          retention-days: 7
