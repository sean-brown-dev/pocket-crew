name: Gemini PR Review

on:
  pull_request:
    types: [opened, synchronize] 

permissions:
  pull-requests: write 

jobs:
  ai-review:
    runs-on: ubuntu-latest 
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        # Vertex AI Key and reasoning stream handling benefit from requests for direct REST control
        run: pip install requests

      - name: Fetch PR Diff
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        # Grabs only the raw patch diff of the changed files
        run: gh pr diff ${{ github.event.pull_request.number }} > pr_diff.txt

      - name: Run Gemini Review
        env:
          VERTEX_API_KEY: ${{ secrets.VERTEX_API_KEY }}
          # Providing defaults but these should ideally be in secrets if they vary
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          GCP_REGION: ${{ secrets.GCP_REGION || 'us-central1' }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: python .github/scripts/gemini_review.py
